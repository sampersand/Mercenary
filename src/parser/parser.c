#include "parser.h"
#include <stdlib.h>

#include "dyn_array.h"

program_t* parse_program(const char* stream) {
    program_t* pgm = NULL;

    // decl_array_t decls;
    // arr_alloc(decls);    

    return pgm;
}